// Simple test to verify Vitest is working
import { test, expect } from 'vitest';

test('1 + 1 equals 2', () => {
  expect(1 + 1).toBe(2);
});

test('1 + 2 equals 3', () => {
  expect(1 + 2).toBe(3);
});
