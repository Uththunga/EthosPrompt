// CommonJS config for Vitest to avoid ESM-related issues
const { defineConfig } = require('vitest/config');

module.exports = defineConfig({
  test: {
    globals: true,
    environment: 'jsdom',
    include: ['**/__tests__/**/*.{test,spec}.{js,jsx,ts,tsx}'],
    exclude: [
      '**/node_modules/**',
      '**/dist/**',
      '**/.next/**',
      '**/.vercel/**',
      '**/.git/**',
      '**/cypress/**',
      '**/playwright-tests/**',
      '**/e2e/**',
      '**/coverage/**',
    ],
    testTimeout: 30000,
    hookTimeout: 10000,
    watch: false,
    clearMocks: true,
    mockReset: true,
    restoreMocks: true,
    passWithNoTests: true,
    logHeapUsage: true,
    isolate: true,
    reporters: ['default'],
  },
});
